export const LIVE_TRIP_GEOFENCE_TASK = 'TRAVEL_GO_LIVE_TRIP_GEOFENCE';
export const LIVE_TRIP_STORAGE_KEY = '@travel/live_trip_v1';
export const LIVE_TRIP_PENDING_KEY = '@travel/live_trip_pending_arrival_v1';

/** Arrival radius around the next stop (meters). */
export const LIVE_TRIP_RADIUS_METERS = 120;

/** Ignore repeat Enter events for the same stop within this window. */
export const LIVE_TRIP_DEBOUNCE_MS = 45_000;
